# Grafana Dashboard 自动加载配置
apiVersion: 1

providers:
  - name: "Audit Platform Dashboards"
    orgId: 1
    folder: ""
    type: file
    disableDeletion: true
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/dashboards
